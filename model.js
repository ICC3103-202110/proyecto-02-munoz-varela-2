initModel = {
    tableInfo: [], // save all the info: city, temp,  min, max.
    nameCities: [], // save all the cities names.
    apiInfo: [] // save the information from API.
}
module.exports = {
    initModel
}